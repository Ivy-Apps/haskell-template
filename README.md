# Haskell template for Ivy Apps

## Setup

1. Install Nix (https://github.com/DeterminateSystems/nix-installer recommended)
2. `nix develop`
3. `make check` to run all checks and build.

## Update dependencies

1. `make update`
2. Commit the new `cabal.project.freeze`
